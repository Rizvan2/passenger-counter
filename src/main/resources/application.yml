server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: passenger-counter
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  datasource:
    url: ${SPRING_DATASOURCE_URL:}
    username: ${SPRING_DATASOURCE_USERNAME:}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.h2.Driver}
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        format_sql: true

ftp:
  port: 2021
  user: admin
  password: admin
  home: ./videos/incoming
  processed-dir: ./videos/processed
  passive:
    ports: 30000-30009
    external-address: ""
  analysis:
    line-y-ratio: 0.5
    inside-on-top: true
    auto-initial-onboard: true
    initial-onboard: 0

pc:
  videos-dir: ./videos   # ← добавить эту строку  models-dir: ./models
  yolo-input-size: 416
  confidence-threshold: 0.35
  nms-iou-threshold: 0.5
  reid-similarity-threshold: 0.65
  track-active-lost-frames: 120
  track-center-distance-threshold-px: 90
  side-fallback-max-distance-px: 1200
  process-every-n-frames: 3
  emit-frame-every-ms: 120
  jpeg-quality: 0.45
  analysis-log-every-n-frames: 30
  door-zone-half-width-ratio: 0.04
  door-zone-min-half-width-px: 10
  door-zone-max-half-width-px: 60
  count-anchor-y-ratio: 0.55
  count-min-anchor-movement-px: 35
  count-center-line-crossing: true

analysis-results:
  persistence:
    enabled: ${ANALYSIS_RESULTS_PERSISTENCE_ENABLED:false}

logging:
  level:
    ru.rtds.pc: DEBUG
    org.bytedeco: WARN
